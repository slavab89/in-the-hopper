{
  "name": "mid-hopper",
  "version": "1.0.0",
  "description": "Node.js middleware for taking out data from a request",
  "main": "index.js",
  "scripts": {
    "commit": "git-cz",
    "commitmsg": "commitlint -e $GIT_PARAMS",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "contributors": [
    "Slava Balabanov <bslava89@gmail.com>"
  ],
  "keywords": [
    "express",
    "koa",
    "connect",
    "http",
    "logger",
    "middleware"
  ],
  "engines": {
    "node": ">= 8"
  },
  "license": "MIT",
  "dependencies": {
    "on-finished": "^2.3.0"
  },
  "devDependencies": {
    "@commitlint/config-conventional": "^6.1.3",
    "commitizen": "^2.9.6",
    "commitlint": "^6.2.0",
    "cz-conventional-changelog": "^2.1.0",
    "eslint": "^4.19.1",
    "eslint-config-airbnb-base": "^12.1.0",
    "eslint-plugin-import": "^2.11.0",
    "husky": "^0.14.3"
  },
  "config": {
    "commitizen": {
      "path": "cz-conventional-changelog"
    }
  },
  "commitlint": {
    "extends": [
      "./@commitlint/config-conventional"
    ]
  }
}
